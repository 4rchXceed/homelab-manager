#!/bin/sh
IP="$(curl https://api.ipify.org)"
# Here I use the dynamic IP of infomaniak, since my IP is dynamic. It's pretty simple
curl "https://infomaniak.com/nic/update?hostname=webdash.dev&myip=$IP&username=your-username&password=your-password"

class EmergencyActionTemplate:
    @staticmethod
    def call(config: dict):
        """
        Calls the emergency action with the given config.
        Here you do what you need to do.
        For examples, see the other emergency action plugins.
        Parameters:
            - config: dict - The configuration for the emergency action. (in emergency_proc.json or other imported config)
        """
        return

services:
    test-app:
        image: nginx:latest
        ports:
            - "5005:80"
        volumes:
            - ./status:/status
        command: ["sh", "-c", 'echo OK > /status && nginx -g "daemon off;"']
